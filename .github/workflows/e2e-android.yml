name: E2E Tests (Android)

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - "app/**"
      - ".github/workflows/e2e-android.yml"
  pull_request:
    branches: [main]
    paths:
      - "backend/**"
      - "app/**"
      - ".github/workflows/e2e-android.yml"

jobs:
  e2e-android:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Build backend
        run: cd backend && go build -o server .

      - name: Start backend
        run: |
          cd backend && ./server &
          for i in $(seq 1 30); do
            if curl -s http://localhost:8080/health > /dev/null 2>&1; then
              echo "Backend is ready"
              break
            fi
            echo "Waiting for backend... ($i)"
            sleep 1
          done

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Generate platform files
        run: cd app && flutter create --project-name dear_baby --org com.dearbaby .

      - name: Remove generated unit tests
        run: rm -rf app/test

      - name: Install dependencies
        run: cd app && flutter pub get

      - name: Enable KVM
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm

      - name: Run integration tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 34
          target: google_apis
          arch: x86_64
          force-avd-creation: false
          emulator-options: -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim
          disable-animations: true
          script: |
            cd app && flutter drive \
              --driver=test_driver/integration_test.dart \
              --target=integration_test/health_test.dart \
              -d emulator-5554 \
              --dart-define=BASE_URL=http://10.0.2.2:8080

      - name: Stop backend
        if: always()
        run: pkill -f "backend/server" || true
