apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  # ClusterIP — redis is never exposed outside the cluster. Backend and
  # worker pods connect over the in-cluster DNS name `redis:6379`.
  selector:
    app: redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
