# Copy to redis-secret.yaml and populate before applying. Consumed by both
# backend and worker. REDIS_URL is split into its own Secret so the credential
# can rotate independently of the OpenRouter key.
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
type: Opaque
stringData:
  REDIS_URL: "redis://redis:6379"
