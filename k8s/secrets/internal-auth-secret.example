# Shared by backend + worker. Authenticates worker → backend internal
# API calls. Rotate both pods in lock-step when rolling this value.
apiVersion: v1
kind: Secret
metadata:
  name: internal-auth-secret
type: Opaque
stringData:
  INTERNAL_API_TOKEN: "REPLACE-ME-with-a-long-random-string"
