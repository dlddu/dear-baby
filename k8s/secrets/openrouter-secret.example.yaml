# Copy to openrouter-secret.yaml and populate with real values before applying.
# Consumed by the worker Deployment only.
apiVersion: v1
kind: Secret
metadata:
  name: openrouter-secret
type: Opaque
stringData:
  OPENROUTER_API_KEY: "sk-or-v1-REPLACE_ME"
