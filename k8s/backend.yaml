apiVersion: apps/v1
kind: Deployment
metadata:
  name: dear-baby-backend
  labels:
    app: dear-baby-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dear-baby-backend
  template:
    metadata:
      labels:
        app: dear-baby-backend
    spec:
      containers:
        - name: dear-baby-backend
          image: ghcr.io/dlddu/dear-baby-backend:latest
          ports:
            - containerPort: 8080
              hostPort: 8080
          volumeMounts:
            - name: dear-baby-sqlite-data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
      volumes:
        - name: dear-baby-sqlite-data
          persistentVolumeClaim:
            claimName: dear-baby-sqlite-data
---
apiVersion: v1
kind: Service
metadata:
  name: dear-baby-backend
  labels:
    app: dear-baby-backend
spec:
  type: ClusterIP
  selector:
    app: dear-baby-backend
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dear-baby-sqlite-data
  labels:
    app: dear-baby-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
